### 📞 Голосовые звонки — исправление звука

- **Исправлена потеря звука на Samsung** — аудио-сессия теперь запускается ДО инициализации WebRTC, что критично для корейских прошивок
- Аудио-фокус запрашивается через современное API (Android 8+), а не устаревший `requestAudioFocus(null)`
- Удалён конфликтующий `react-native-incall-manager`, который вмешивался в управление аудио
- TURN-сервер на VPS теперь корректно передаётся клиентам — звонки работают даже за симметричным NAT

### 🔗 Стабильность

- Исправлен скрипт обновления сервера — TURN-конфигурация гарантированно подхватывается при перезапуске
