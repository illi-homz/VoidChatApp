### 🎨 Интерфейс

- **Тосты полностью переработаны** — теперь во всю ширину с отступами 16px, тёмный непрозрачный фон, иконки статуса (✅ успех, ✖ ошибка, ⚠ предупреждение, 💬 инфо), плавные Reanimated-анимации (FadeInUp/FadeOutUp)
- **Иконки в шторке с экшенами** — у каждого пункта меню появилась иконка: подключение, переименование, удаление, отмена и т.д.
- **Шторку теперь можно тащить за ручку** — исправлена зона захвата PanResponder
- **Заголовок в шторке** увеличен до 24px
- **Фон шторок** стал чуть прозрачнее (0.7 → 0.55)
- **Убран дублирующийся заголовок** с экранов «Добавить контакт» и «Новый сервер» — остался только на странице
- **Кнопка QR-сканера** — теперь только иконка (без текста), расположена слева от кнопки отправки приглашения
- **Новая иконка «метла»** для очистки чата — ведьмина метла под наклоном
- **Сплэш-экран** — статус-бар скрыт во время показа, угловые скобки не перекрываются

### ⌨️ Ввод

- **Авто-точка при вводе IP** — после каждых 3 цифр точка подставляется сама
- **Цифровая клавиатура** для поля IP
- **Клавиатура больше не перекрывает поля** при редактировании сервера

### ⚙️ Функции

- **Редактирование IP и порта сервера** — новый пункт в меню долгого нажатия на сервер

### 🐛 Исправления

- **Шторка снова открывается после смахивания** — добавлен вызов onClose при drag-жесте
- **Плавное появление маркеров выделения** в чате — ширина анимируется через RN Animated, без рывков
